// Styles for for Hero Component
import styled from 'styled-components'

export const HeroParagraph = styled.p`
  font-size: .92rem;
  line-height: 1.4rem;
  width: 100%;
  max-width: 28rem;

  &:last-of-type {
    padding-bottom: calc((3.16rem - 2rem) + 0.65rem);
  }
`